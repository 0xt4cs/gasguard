# GasGuard Environment Configuration
# Copy this file to .env and update with your values
# IMPORTANT: Never commit .env to git (already in .gitignore)

# =============================================================================
# APPLICATION SETTINGS
# =============================================================================

# Node Environment (development | production)
NODE_ENV=production

# Server Port (default: 3000)
PORT=3000

# =============================================================================
# SECURITY SETTINGS (CRITICAL - CHANGE IN PRODUCTION!)
# =============================================================================

# JWT Secret Key for authentication tokens
# Generate a strong random key using this command:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-change-this-to-random-64-char-hex-string

# JWT Token Expiration (7d = 7 days, suitable for local IoT device)
JWT_EXPIRES_IN=7d

# =============================================================================
# SMS CONFIGURATION (TextBee)
# =============================================================================

# Note: TextBee configuration is managed via Web UI (Settings → SMS Configuration)
# These values are stored in the database, not in .env
# Get your credentials from: https://textbee.dev/dashboard
# After first setup, configure via: http://raspberry-pi-ip:3000/settings

# =============================================================================
# DATABASE SETTINGS
# =============================================================================

# SQLite Database Path (default: ./data/gasguard.db)
# DB_PATH=./data/gasguard.db

# Data Retention Period in Days (default: 90)
# DATA_RETENTION_DAYS=90

# Data Retention Warning Threshold in Days (default: 14)
# DATA_RETENTION_WARNING_DAYS=14

# =============================================================================
# HARDWARE SETTINGS (Advanced - Optional)
# =============================================================================

# Sensor Reading Interval in milliseconds (default: 2000 = 2 seconds)
# SENSOR_READING_INTERVAL=2000

# Sensor Preheat Time in seconds (default: 30)
# SENSOR_PREHEAT_TIME=30

# =============================================================================
# LOGGING (Optional)
# =============================================================================

# Log Level (error | warn | info | debug)
# LOG_LEVEL=info

# Enable Detailed Hardware Logging (true | false)
# DEBUG_HARDWARE=false

# =============================================================================
# NOTES
# =============================================================================

# 1. Copy this file: cp env.example .env
# 2. Generate JWT_SECRET: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# 3. Update JWT_SECRET in .env with the generated value
# 4. Never commit .env to version control
# 5. Restart server after changing .env: npm start

